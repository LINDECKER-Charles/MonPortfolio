# Env for the VPS-global edge proxy. Copy to .env (git-ignored) on the host.
#   docker compose --env-file .env up -d

# Let's Encrypt contact for every certificate the edge issues (all projects).
ACME_EMAIL=admin@example.com
